---
- name: Copy .md files to db
  hosts: db
  become: true
  tasks:
    - name: Copying the files
      ansible.builtin.copy:
        src: "{{ item }}"
        dest: /home/deploy
        owner: deploy
        mode: "0644"
      with_fileglob:
        - "files/*.md"
